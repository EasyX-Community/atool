
function delAllLogs() {
  echo "--------------- ⏲  Clearing logs... ---------------"

  # Clear logs.
  find /var/log/ -type f -name "*.log*" -execdir truncate --size=0 {} \;

  echo "--------------- ⏲  Deleting .gz log files... ---------------"

  # Delete .gz files.
  find /var/log/ -type f -regex ".*\.gz$" -delete

  echo "--------------- 💯 DONE: NGINX logs cleared ... ---------------"
}

delAllLogs
